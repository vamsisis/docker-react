{{ define "loadbalancer" -}}
{{ if gt (len .Alerts.Firing) 0 -}}
🚨 Firing alert(s): 
Alert: {{ .CommonLabels.alertname }}
Load Balancer: {{ with index .Alerts.Firing 0 -}}{{ .Labels.LoadBalancer | default "[Unknown]" }}{{ end }}
{{ else if gt (len .Alerts.Resolved) 0 -}}
✅ Resolved alert(s): 
Alert: {{ .CommonLabels.alertname }}
Load Balancer: {{ with index .Alerts.Resolved 0 -}}{{ .Labels.LoadBalancer | default "[Unknown]" }}{{ end }}
{{ end -}}
{{ end -}}
