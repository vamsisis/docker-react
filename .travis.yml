language: generic
sudo: required
services:
    - docker 

before_install:
    - docker build -t vamsisis/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true vamsisis/react-app npm run test -- --coverage

